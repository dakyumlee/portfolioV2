spring:
  application:
    name: portfolio
  
  datasource:
    url: jdbc:postgresql://localhost:5432/portfolio
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  
  flyway:
    enabled: true
    baseline-on-migrate: true
  
  thymeleaf:
    cache: false
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${GMAIL_USERNAME:oicrcutie@gmail.com}
    password: ${GMAIL_APP_PASSWORD:fcbaxhdasjcxpfyt}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

denguklab:
  admin:
    username: ${ADMIN_USERNAME:oicrcutie}
    password: ${ADMIN_PASSWORD:aa667788!!}

---
spring:
  config:
    activate:
      on-profile: prod
  
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/portfolio}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${PORT:8080}